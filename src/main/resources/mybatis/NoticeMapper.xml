<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.NoticeMapper">
    <insert id="insertNotice" parameterType="com.example.demo.dto.NoticeDto" keyProperty="noticeId">
        INSERT INTO NOTICE(
        NOTICE_ID,
        TITLE,
        CONTENT,
        WRITER)
        VALUES(NOTICE_SEQ.NEXTVAL,
        #{title},#{content},#{writer})
    </insert>

    <!-- 공지목록 + 검색 + 페이지-->
    <select id="selectNoticeList" resultType="NoticeDto">
        SELECT *
        FROM NOTICE
        WHERE TITLE LIKE '%' || #{keyword}|| '%'
        ORDER BY PIN_YN DESC,NOTICE_ID DESC
        OFFSET #{OFFSET}ROWS FETCH NEXT #{size} ROWS ONLY
    </select>

    <!-- 전체 개수-->
    <select id="selectNoticeCount" resultType="int">
        SELECT COUNT(*)
        FROM NOTICE
        WHERE TITLE LIKE '%' || #{keyword} || '%'
    </select>

    <!-- 공지상세-->
    <select id="selectNoticeDetail" resultType="NoticeDto">
        SELECT * FROM NOTICE
        WHERE NOTICE_ID = #{noticeId}
    </select>
    <!-- 조회수 증가-->
    <update id="increaseViewCount">
        UPDATE NOTICE
        SET VIEW_COUNT=VIEW_COUNT + 1
        WHERE NOTICE_ID = #{noticeId}
    </update>

</mapper>
