<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.NoticeMapper">
    <insert id="insert" parameterType="com.example.demo.dto.NoticeDto" keyProperty="noticeId">
        INSERT INTO NOTICE(
        NOTICE_ID,
        TITLE,
        CONTENT,
        WRITER)
        VALUES(NOTICE_SEQ.NEXTVAL,
        #{title},#{content},#{writer})
    </insert>
<!--    <select id="findAll"  resultType="com.example.demo.dto.NoticeDto">-->
<!--        SELECT-->
<!--        NOT-->
<!--        -->
<!--    </select>-->
<!--    SELECT * FROM NOTICE WHERE NOTICE_ID = #{noticeId}&ndash;&gt;-->
<!--&lt;!&ndash;    </select>&ndash;&gt;-->
<!--&lt;!&ndash;    &ndash;&gt;-->

</mapper>
